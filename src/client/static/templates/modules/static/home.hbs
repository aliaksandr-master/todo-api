<div class="welcome">
    <h1>{{t 'static-home.welcome_to'}} <b>Listslider</b></h1>
    <div class="clearfix welcome-btns">
        <a href="{{url 'user-register'}}" class="btn btn-lg btn-warning">{{t 'static-home.try_service'}}</a>
        <a href="{{url 'user-login'}}" class="btn btn-lg btn-success">{{t 'static-home.sign_in'}}</a>
    </div>
</div>